apiVersion: v1
kind: Service
metadata:
  name: {{ include "concept-car-fy27.fullname" . }}
  labels:
    {{- include "concept-car-fy27.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "concept-car-fy27.selectorLabels" . | nindent 4 }}
